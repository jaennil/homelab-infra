apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: landing-tls
  namespace: landing
spec:
  secretName: landing-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - dubrovskih.ru
    - www.dubrovskih.ru
