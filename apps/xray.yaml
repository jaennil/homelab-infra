apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: xray
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/jaennil/homelab-infra
    targetRevision: main
    path: xray
  destination:
    server: https://kubernetes.default.svc
    namespace: proxy
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
